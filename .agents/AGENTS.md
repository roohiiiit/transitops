# Workspace Rules

- **Do Not Push Directly:** Never run `git push` directly without asking for explicit approval first, or unless instructed to do so. Prefer committing changes locally and allowing the user to review before pushing.
